@using System.Linq
@using AutoRest.Java
@inherits AutoRest.Core.Template<AutoRest.Java.Model.PackageInfoTemplateModel>
@Header("// ").TrimMultilineHeader()
@EmptyLine

/**
@if (string.IsNullOrEmpty(Model.SubPackage))
{
@: * This package contains the classes for @(Model.Title).
}
else
{
@: * This package contains the @Model.SubPackage classes for @(Model.Title).
}
@if (!string.IsNullOrEmpty(Model.Description))
{
@: * @Model.Description.Period()
}
 */

@if (string.IsNullOrEmpty(Model.SubPackage))
{
@:package @(Settings.Namespace.ToLower());
}
else
{
@:package @(Settings.Namespace.ToLower()).@(Model.SubPackage);
}