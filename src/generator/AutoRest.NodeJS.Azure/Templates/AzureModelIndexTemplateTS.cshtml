@using System.Linq
@using AutoRest.NodeJS.Templates
@using AutoRest.NodeJS.Azure.Templates
@using AutoRest.NodeJS.Azure.TemplateModels
@inherits AutoRest.Core.Template<AzureServiceClientTemplateModel>
/*
@Header(" * ")
 */
@EmptyLine
@foreach (var model in Model.OrderedModelTemplateModels) {
@EmptyLine
@:@(Include(new ModelTemplateTS(), model))
}
@EmptyLine
@foreach (var model in Model.PageTemplateModels)
{
@EmptyLine
@:@(Include(new PageModelTemplateTS(), model))
}
