@using AutoRest.Core
@inherits AutoRest.Core.Template<AutoRest.Ruby.Model.EnumTypeRb>
# encoding: utf-8
@Header("# ")
@EmptyLine
module @Settings.Namespace
  module Models
    #
    @WrapComment("# ", "Defines values for " + Model.ModuleName)
    #
    module @(Model.ModuleName)
      @for (int i = 0; i < Model.Values.Count; i++)
      {
      @:@(CodeNamer.Instance.PascalCase(CodeNamer.Instance.RemoveInvalidCharacters(Model.Values[i].Name))) = "@(Model.Values[i].SerializedName)"
      }
    end
  end
end
