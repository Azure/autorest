@inherits AutoRest.Core.Template<System.Collections.Generic.IEnumerable<AutoRest.CSharp.LoadBalanced.Legacy.Model.MethodCs>>
namespace @Settings.Namespace
{
    public static class Metrics
    {
        public const string Prefix = "@(Settings.Namespace).API.";
    @foreach (var parameter in Model)
    {
@:        public const string @(parameter.Name) = Prefix + "@(parameter.NameWithoutRoute)";
    }
    }
}